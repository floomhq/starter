# Claude Skill Page Handoff

Open `index.html` first — it redirects to the canonical **`../skill.html`** (single skills browser).

## Included Files

- `index.html` - redirect shim (some tools still expect `index.html` here).

Starter pack zip (used by Skills CTAs) lives under **`../../assets/`** from this folder (`Floom/assets/`).

## What To Preserve

- Download-first v0 positioning.
- No auto-sync promise.
- Focused skills pack language.
- The Swiss/SaaS visual style from the existing page.

## Suggested Prompt

Improve the Floom skills browser. Use **`../skill.html`** (repo root within `Floom/`) as the source of
truth. Keep the page focused on downloading a starter skills pack and do not
introduce an auto-sync launch promise.
